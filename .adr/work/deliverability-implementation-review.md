# Code review: deliverability T1–T4 implementation

## Review constraints

| Axis             | Selection                                                                                                                                                                                                                                                                                                                  |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Target           | Uncommitted deliverability implementation vs git `cb73c3a` / `main`. Branch `deliverability`. Files of interest listed under Coverage.                                                                                                                                                                                     |
| Baseline         | Plan-backed. Authority: `.adr/work/deliverability.md` (full document, T1–T4, Final acceptance, Handoff deviations) and ADR-0010. Supporting accepted ADRs 0002 and 0009 constrain lifecycle-header surgery and the `:29` rationale freeze. Plan-review writeups and task Status labels are claims/evidence, not authority. |
| Scope            | Full plan vs implementation (T1 DNS + T2 MAIL FROM update + T3 live gate + T4 docs). Required callers, contracts, shared state, docs, config, wiki.                                                                                                                                                                        |
| Invocation       | Standalone adversarial implementation review. Fresh judgment; prior plan-review closure is not proof the implementation is correct.                                                                                                                                                                                        |
| Output           | `.adr/work/deliverability-implementation-review.md` only. No implementation, ADR-body, README, wiki, or work-document edits. No VCS mutation.                                                                                                                                                                              |
| Dimensions       | Correctness (identity update vs replace, MAIL FROM props, alignment tokens), libraries (Alchemy beta.77 `EmailIdentity` diff/reconcile), ops/DNS, docs/ADR lifecycle, tests/validation, out-of-scope creep.                                                                                                                |
| Validation/tools | `git status` / `git diff` vs `cb73c3a`; source reads of `stacks/sending-identity.ts` and installed `EmailIdentity.ts` / `Plan.ts` / `Diff.ts`; T4 greps; `pnpm format:check`; oxlint on the stack file; Python relative-link check; public `dig` of the four T1 names.                                                     |
| Writes/artifacts | This report only.                                                                                                                                                                                                                                                                                                          |

## Summary

T2's two MAIL FROM props and header comment match the plan exactly. Alchemy's provider still replaces only on an identity-name change and applies MAIL FROM through `putEmailIdentityMailFromAttributes` without touching DKIM. The four DNS records resolve at the authoritative server and at `8.8.8.8` with the specified values. T4 ADR/README/wiki/`.env.example` surgery matches the Change text, including the ADR-0009 `:29` freeze. T3's recorded `Authentication-Results` paraphrases contain every required token and do not contradict the gate; raw headers and SES were not re-read.

The "six vs seven" retained-record count is a clerical correction of an internal inconsistency already present in accepted ADR-0010 (the enumeration listed seven records while the count said six). README lists those seven without claiming six.

No material findings. Live T2 plan/deploy/SES reads and T3 the operator mailbox/destroy inventory were not re-probed (forbidden / not on the allowed re-run list). Those rows are **Unverifiable** from this review.

**Closure: Clear.**

## Related decomplex review

- **Report:** [deliverability-decomplex.md](deliverability-decomplex.md)
- **Owner disposition summary:** No potential complexity findings. Not re-opened; this implementation added two props, four hand-published records, and the T4 record-keeping the plan named.

## Coverage

### Inspected

- `.adr/work/deliverability.md` (Outcome, In/Out of scope, Key files, Research, T1–T4 Change/Evidence/Tests/Verify, Final acceptance, Handoff deviations)
- `.adr/0010-aligned-mail-from-spf-and-dmarc.md` (Accepted; baseline vs working tree)
- `.adr/0002-domain-sending-identity.md` header (lifecycle line) and confirmation that the body is unchanged
- `.adr/0009-account-level-sending-identity.md` header, `:29`, `:47`, `:49`
- `stacks/sending-identity.ts` (working tree and `cb73c3a` baseline)
- Alchemy beta.77: `EmailIdentity.ts:56-69, 199-211, 260-273, 275-284, 371-386`; `Plan.ts:1511-1519`; `Diff.ts:184-201`
- `README.md` Sending identity (`:174-198`) and teardown retained-inventory sentence (`:255`)
- `.env.example:9-13`
- `wiki/aws/deliverability.md:12-20, 28`; `wiki/aws/ses.md:105`
- `git status --short`, `git diff cb73c3a`, `git log -5 --oneline`
- `.adr/work/deliverability-review.md` (plan review R1–R8; used as historical context, not as proof of this implementation)
- `.env.test` identity and first allowlist entry only (secrets not copied here)

### Skipped or partial

- `alchemy plan` / `deploy` / `destroy` / `state` (deploy forbidden; plan skipped because it can bootstrap state)
- `aws sesv2 get-email-identity`, CloudTrail, Route 53 change-batch history, Cloudflare API reads
- Sending mail; the operator mailbox header or `envelopeFrom` re-read
- T3 live re-deploy / destroy inventory
- Full `pnpm check` (470 unit tests). `pnpm format:check` and oxlint on the stack file were run
- Historical work documents named as left unchanged
- Non-identity values and secrets in `.env.test`
- Mass-sending worktree contents beyond this tree's `git diff` (no `apps/` or `alchemy.run.ts` change here)

### Required boundaries

- Only `AWS.SES.EmailIdentity(` call in app/stacks remains the owner stack; outputs still `{ emailIdentity, dkimTokens }`
- No new config key; `mailFromDomain` is `bounce.${emailIdentity}` from `EMAILER_SENDER_IDENTITY`
- `mailFromBehaviorOnMxFailure` is the literal `"USE_DEFAULT_VALUE"`
- ADR-0002 body (including `:40-45`) is unchanged; only the header lifecycle line was added
- ADR-0009 `:29` still reads "The three DKIM `CNAME` records are published **once**"
- No application, Emailer-stack, Route 53 resource, BIMI, ARC, dedicated-IP, or VDM change in this diff
- `Mailer.ts` still maps `MailFromDomainNotVerifiedException` → `identity-not-verified` (untouched)

## Validation

- **Run:**
  - `git status --short` / `git diff cb73c3a` / `git rev-parse HEAD` — HEAD `cb73c3a` = `main`. Tracked edits only: ADRs 0002/0009/0010, `.adr/work/deliverability.md`, `.env.example`, `README.md`, `stacks/sending-identity.ts`, `wiki/aws/deliverability.md`, `wiki/aws/ses.md`. `git diff cb73c3a -- apps alchemy.run.ts` empty.
  - T4 grep `only prop|Still open from ADR-0002` over `.adr/0009-*.md` `README.md` `.env.example` — no match. `grep -n "three DKIM" README.md` — no match.
  - `pnpm format:check` — 117 files, clean.
  - `pnpm exec oxlint --type-aware --type-check --deny-warnings stacks/sending-identity.ts` — 0 warnings / 0 errors.
  - Python relative-link check over `README.md`, `.adr/**/*.md`, `wiki/**/*.md` — **379** links resolved, **0** broken (ellipsis placeholders skipped). Work-document claim was 385; the zero-broken result is what T4 Verify requires.
  - `.env.test`: `EMAILER_SENDER_IDENTITY=mail.example.com`; first `EMAILER_ALLOWED_RECIPIENTS` entry `dmarc@reports.example.net`.
  - Public `dig` (the zone's authoritative Route 53 name server, then `@8.8.8.8`):
    - `bounce.mail.example.com MX` → `10 feedback-smtp.us-east-1.amazonses.com.` and nothing else (TTL 1800).
    - `bounce.mail.example.com TXT` → `"v=spf1 include:amazonses.com ~all"`.
    - `_dmarc.mail.example.com TXT` → `"v=DMARC1; p=none; rua=mailto:dmarc@reports.example.net"` (no `aspf`, no `sp`).
    - `mail.example.com._report._dmarc.reports.example.net TXT` `@8.8.8.8` → `"v=DMARC1"`.
- **Skipped/unavailable:**
  - `alchemy plan --config stacks/sending-identity.ts --stage shared --detailed` (parent claim: `Plan: 1 to update`, `[EmailerSender] update`, no `replace`/`create`)
  - `alchemy deploy` / `alchemy state read EmailerSending/shared/EmailerSender`
  - `aws sesv2 get-email-identity` (parent claim: `MailFromDomain=bounce.mail.example.com`, `BehaviorOnMxFailure=USE_DEFAULT_VALUE`, `MailFromDomainStatus=SUCCESS`, DKIM `SUCCESS`, `LastKeyGenerationTimestamp=2026-09-14T17:30:33.252000+02:00`)
  - Route 53 change `C00505981QJHOM2GPEVP4` / Cloudflare record `996789d5a2305861f26a2eb5b93957f2` / API preflights
  - T3 Emailer `test-a` plan/deploy/send/destroy, the operator mailbox `in_c87e4714…`, raw `Authentication-Results` block
  - Full `pnpm check` (parent claim: 470 unit tests)

## Plan-backed verdicts

1. **Plan/baseline quality and omissions:** The accepted plan is specific and testable: API-only preflight, `CREATE` not `UPSERT`, records before the MAIL FROM attribute, exact two props, `Plan: 1 to update` with no replace, DKIM timestamp freeze, five named header assertions on `Authentication-Results: mx.cloudflare.net;`, ADR lifecycle surgery that freezes ADR-0009 `:29`, and T4 greps. Out of scope (app/Emailer/mass-sending, `p=reject`, Alchemy DNS, BIMI/ARC/VDM) is explicit. Residual baseline limits: T3 is live-only and its Evidence stores paraphrases rather than a raw header dump; T4 Change/Final acceptance still say "six" records while enumerating seven (the same off-by-one ADR-0010 already carried); T4's "user accepts" after T3 is a process conjunct this review cannot independently witness. None of those blocked a faithful implementation. **Confidence:** high on the written baseline.

2. **Implementation compliance:** T1 current DNS Verify, T2 declaration, T4 docs, and out-of-scope absence are **Complete**. T2 live plan/SES/state and T3 live send/destroy are **Unverifiable** here (not re-run); recorded T3 tokens match the five assertions and do not contradict them. The six→seven count is an **Approved deviation** (clerical correction of accepted ADR-0010's internal inconsistency; README lists seven without claiming six). No Incorrect, Missing, or Overbuilt rows. Work-document T4 Status still "In progress" is tracker lag, not missing files. **Confidence:** high on source/docs/current DNS; low on unreproduced live AWS/the operator mailbox facts.

3. **Implementation quality beyond the baseline:** No material defect found. The only code change is the two specified props plus the specified comment; outputs, logical id, `RemovalPolicy.retain()`, and `AWS.state()` are untouched. README still says deploy-then-publish (needed for DKIM tokens; MAIL FROM records do not need the identity first). That is the greenfield order T1/T2 were reordered to avoid, but T4 did not require a README reorder, the readiness sentence still requires `MailFromDomainStatus=SUCCESS`, and `USE_DEFAULT_VALUE` keeps mail flowing. Not admitted. Easy DKIM tokens being unchanged would not prove "no recreate" (they are deterministic per domain); the recorded `LastKeyGenerationTimestamp` is the actual signal, and it is recorded as frozen. **Confidence:** high for in-scope source/docs; live SES not re-observed.

4. **Test and validation quality:** T1/T2/T4 correctly have no new unit tests. T3 is the live gate and was not re-run. This review independently confirmed the four DNS answers, T4 greps, format, stack-file oxlint, and relative links. T3 Evidence quotes the required tokens with message id `in_c87e4714…` and SES `messageId` `010001a0a3fb7559-…` but does not paste the raw header block; that is a confidence limit, not a contradiction. Parent `pnpm check` (470) was not re-run. **Confidence:** high for DNS/docs gates; T3 live remains parent-recorded.

## Plan compliance matrix

| Authority item / implied requirement                                                                                                                                                                                                                           | Expected evidence                                                                                        | Implementation evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Validation / test evidence                                                | Status                                                                                |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Outcome: delivered mail shows `spf=pass` with `smtp.mailfrom` at `bounce.mail.example.com`, `dkim=pass header.d=mail.example.com`, `dmarc=pass header.from=mail.example.com`, `p=none`                                                                      | T3 header tokens + DMARC TXT                                                                             | T3 Evidence `:128-129` records those tokens. DMARC TXT currently `"v=DMARC1; p=none; rua=mailto:dmarc@reports.example.net"`.                                                                                                                                                                                                                                                                                                                                                                                  | This review did not re-read the operator mailbox. `dig` confirms `p=none` and no `aspf`. | **Complete** on recorded T3 paraphrases + current DNS. Live headers **Unverifiable**. |
| In scope: four hand-published records, two MAIL FROM props, one live gate, ADR/README/wiki/`.env.example`                                                                                                                                                      | Those artifacts only                                                                                     | `git diff --stat cb73c3a`: 9 files, 74/21. Code change is only `stacks/sending-identity.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                     | `git diff cb73c3a -- apps alchemy.run.ts` empty                           | **Complete**                                                                          |
| Out of scope: no DMARC tightening, no Alchemy DNS, no BIMI/ARC/dedicated IPs/VDM, no app/Emailer/mass-sending change, no DMARC-report handling                                                                                                                 | Absence                                                                                                  | No `AWS.Route53.Record`, no `p=quarantine`/`reject`, no app diff. `Mailer.ts:40` mapping untouched.                                                                                                                                                                                                                                                                                                                                                                                                              | Diff + grep                                                               | **Complete**                                                                          |
| T1: explicit go-ahead naming Route 53 `example.com` (`Z00000000000000`) and Cloudflare `reports.example.net`                                                                                                                                                     | Recorded confirmation                                                                                    | Evidence `:70` names both zones and Cloudflare zone id `00000000000000000000000000000000`.                                                                                                                                                                                                                                                                                                                                                                                                                       | Not independently witnessed                                               | **Unverifiable** (process conjunct)                                                   |
| T1: API-only preflight; no `dig` of names about to be created; `CREATE` never `UPSERT`; TTL 1800                                                                                                                                                               | Preflight `[]` / change id / Cloudflare id                                                               | Evidence `:72-76` claims Route 53 `[]`, Cloudflare count 0, no pre-create `dig`, change `/change/C00505981QJHOM2GPEVP4` `CREATE` → `INSYNC`, Cloudflare TXT id `996789d5a2305861…` `proxied: false` TTL 1800. Live MX TTL is 1800.                                                                                                                                                                                                                                                                               | AWS/Cloudflare APIs not re-read. Current MX TTL 1800 observed.            | **Complete** for current TTL. CREATE/preflight **Unverifiable**.                      |
| T1 Verify: four records at authoritative then `8.8.8.8`                                                                                                                                                                                                        | Exact MX/SPF/DMARC/report answers                                                                        | Live `dig` matches T1 Verify verbatim; MX has no extra answers.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | This review's `dig`                                                       | **Complete**                                                                          |
| T2: `mailFromDomain: \`bounce.${emailIdentity}\``and`mailFromBehaviorOnMxFailure: "USE_DEFAULT_VALUE"`on the existing`EmailIdentity`; no other declaration/output change; no new config key                                                                    | Those two props only                                                                                     | `stacks/sending-identity.ts:25-34`. Outputs still `emailIdentity` / `dkimTokens`. Identity still `Config.string("EMAILER_SENDER_IDENTITY")`.                                                                                                                                                                                                                                                                                                                                                                     | oxlint 0/0 on the file                                                    | **Complete**                                                                          |
| T2: header comment that the MAIL FROM subdomain sends nothing, receives only SES feedback at its MX, and must never be a From address                                                                                                                          | One comment                                                                                              | `stacks/sending-identity.ts:6-7`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Read                                                                      | **Complete**                                                                          |
| Implied: identity is updated, never replaced; DKIM untouched                                                                                                                                                                                                   | Provider `diff` replace-only on name change; reconcile 3e vs 3b/3c; plan `1 to update`; timestamp frozen | `EmailIdentity.ts:275-284` returns `replace` only when `emailIdentity` changes, else `undefined` → `Plan.ts:1511-1519` + `havePropsChanged` → `update`. Reconcile 3e (`:371-386`) calls `putEmailIdentityMailFromAttributes`. 3b/3c run only when DKIM props are declared (they are not). Evidence `:99-102` claims `Plan: 1 to update`, `[EmailerSender] updated`, timestamp `2026-09-14T17:30:33.252000+02:00` unchanged through SUCCESS. Same three DKIM tokens are **not** proof (tokens are deterministic). | Alchemy plan/SES not re-run. Source supports update-not-replace.          | **Complete** (source). Live plan/timestamp **Unverifiable**.                          |
| T2 Verify: `MailFromDomainStatus=SUCCESS`, behaviour `USE_DEFAULT_VALUE`, `removalPolicy: "retain"`                                                                                                                                                            | SES + state read                                                                                         | Evidence `:101-103`. `RemovalPolicy.retain()` still on the declaration (`:29`).                                                                                                                                                                                                                                                                                                                                                                                                                                  | SES/state not re-read                                                     | **Complete** (source retain). Live MAIL FROM status **Unverifiable**.                 |
| T2: do not deploy while mass-sending `test-b` plan/deploy in flight                                                                                                                                                                                            | Recorded check                                                                                           | Evidence `:97` claims no `test-a`/`test-b` Lambdas and no in-flight alchemy.                                                                                                                                                                                                                                                                                                                                                                                                                                     | Not re-probed                                                             | **Unverifiable**                                                                      |
| T3 Tests: `Authentication-Results: mx.cloudflare.net;` contains `spf=pass` with `smtp.mailfrom=<id>@bounce.mail.example.com` (baseline amazonses.com)                                                                                                         | That token                                                                                               | Evidence `:129`: `spf=pass` with `smtp.mailfrom=…@bounce.mail.example.com`. Local-part elided; domain matches.                                                                                                                                                                                                                                                                                                                                                                                                  | the operator mailbox not re-read                                                         | **Complete** (recorded paraphrase). Raw header **Unverifiable**.                      |
| T3 Tests: `dkim=pass header.d=mail.example.com`                                                                                                                                                                                                               | That token                                                                                               | Evidence `:129` exact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Not re-read                                                               | **Complete** (recorded). Raw header **Unverifiable**.                                 |
| T3 Tests: `dmarc=pass header.from=mail.example.com` (baseline `dmarc=none … policy.dmarc=none`)                                                                                                                                                               | That token                                                                                               | Evidence `:129`: `dmarc=pass header.from=mail.example.com policy.dmarc=none`. Extra `policy.dmarc=none` is consistent with `p=none` and is the change from baseline `dmarc=none`.                                                                                                                                                                                                                                                                                                                               | Not re-read                                                               | **Complete** (recorded). Raw header **Unverifiable**.                                 |
| T3 Tests: the operator mailbox `envelopeFrom` or `Received-SPF envelope-from=` at `bounce.mail.example.com`                                                                                                                                                                  | Either field                                                                                             | Evidence `:128-129` records both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Not re-read                                                               | **Complete** (recorded). Live **Unverifiable**.                                       |
| T3 Tests: `DKIM-Signature` `d=mail.example.com` `h=` still includes `List-Unsubscribe:List-Unsubscribe-Post`                                                                                                                                                  | That coverage                                                                                            | Evidence `:129`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Not re-read                                                               | **Complete** (recorded). Raw header **Unverifiable**.                                 |
| T3: ephemeral `test-a` deploy without `EmailIdentity` create; destroy leaves no `test-a` resources; identity not in the destroy plan                                                                                                                           | Plan/inventory                                                                                           | Evidence `:125, :130` claims `Plan: 15 to create` with no `EmailIdentity` create; destroy `Plan: 15 to delete`, identity absent; no leftover `emailer-test-a-*` / `Emailer-*-test-a-*`; `alchemy state list Emailer` path missing. Timestamp still frozen after destroy.                                                                                                                                                                                                                                         | Not re-run                                                                | **Unverifiable**                                                                      |
| T3: `campaigns send` `accepted` / exit 0 (allowlist path still in force)                                                                                                                                                                                       | CLI result                                                                                               | Evidence `:127` `submission.state: "accepted"`, `messageId` `010001a0a3fb7559-…`. `.env.test` still lists `dmarc@reports.example.net` first.                                                                                                                                                                                                                                                                                                                                                                  | Send not re-run                                                           | **Unverifiable** (live). Allowlist first-entry **Complete**.                          |
| T4: ADR-0010 `Status: Accepted` with `Accepted:` date after T3; Confirmation records the gate                                                                                                                                                                  | Header + Confirmation                                                                                    | `Accepted: 2026-09-15` added. Confirmation `:48` records T3 passed with the three verdicts. Status was already `Accepted` at plan-accept time (`cb73c3a`). Separate post-T3 user acceptance not independently witnessed.                                                                                                                                                                                                                                                                                         | Diff vs `cb73c3a`                                                         | **Complete** (files). User-accept conjunct **Unverifiable**.                          |
| T4: ADR-0002 header `- Obligations discharged in part: ADR-0010` naming SPF, DMARC and custom MAIL FROM; body unchanged                                                                                                                                        | One header line; body identical                                                                          | `.adr/0002-domain-sending-identity.md:6`. `git diff` is that single added line. Body `:40-45` still the preserved obligations text.                                                                                                                                                                                                                                                                                                                                                                              | Diff                                                                      | **Complete**                                                                          |
| T4: ADR-0009 `Extended by: ADR-0010`; `:29` stays; `:47` becomes the SPF/DMARC pointer; `:49` drops "only"                                                                                                                                                     | Those four edits                                                                                         | Header `:8`. `:30` still "The three DKIM `CNAME` records are published **once**". `:48` `**SPF and DMARC.** Published under ADR-0010; see there for the records.` `:50` `the prop whose change plans a replacement`.                                                                                                                                                                                                                                                                                             | Read + T4 greps                                                           | **Complete**                                                                          |
| T4 README Sending identity: list the records with values and zones; `MailFromDomainStatus=SUCCESS`; bounce must not be a From domain; readiness `spf=pass` at bounce, `dkim=pass`, `dmarc=pass`                                                                | Rewritten section                                                                                        | `README.md:176-197`. Five bullets = three DKIM CNAMEs + MX + SPF TXT + DMARC TXT + Cloudflare TXT (seven records, two zones). Readiness `:178`. Bounce rule `:197`.                                                                                                                                                                                                                                                                                                                                              | Diff vs baseline `:174-190`                                               | **Complete** (under the six→seven deviation)                                          |
| T4 README teardown `:246-248`: "its DNS records in both zones" instead of "its three DKIM CNAMEs"                                                                                                                                                              | That sentence                                                                                            | `README.md:255`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | T4 grep `three DKIM` on README empty                                      | **Complete**                                                                          |
| T4 `.env.example:11`: "the identity name is the prop whose change plans a replacement"                                                                                                                                                                         | That comment                                                                                             | `.env.example:11-12`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | T4 grep `only prop` empty                                                 | **Complete**                                                                          |
| T4 wiki deliverability: concrete MAIL FROM values, SES verifies MX only, relaxed alignment / never `aspf=s`, `_report._dmarc` rule, `p=none` starting point, negative-cache (publish before probe; authoritative first); `:26` bounce subdomain carries one MX | Those statements                                                                                         | `wiki/aws/deliverability.md:12-14, 18, 20, 28`                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Diff                                                                      | **Complete**                                                                          |
| T4 `wiki/aws/ses.md:105`: `MailFromDomainNotVerifiedException` reachable only under `REJECT_MESSAGE`                                                                                                                                                           | That note                                                                                                | `wiki/aws/ses.md:105`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Diff                                                                      | **Complete**                                                                          |
| T4 Verify greps and format/links                                                                                                                                                                                                                               | Empty greps; format clean; links resolve                                                                 | Greps empty. `pnpm format:check` clean. 379/0 relative links.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | This review                                                               | **Complete**                                                                          |
| Final acceptance: `pnpm check` green                                                                                                                                                                                                                           | Green check                                                                                              | Parent claim 470 tests. This review ran format + stack oxlint only.                                                                                                                                                                                                                                                                                                                                                                                                                                              | Full check not re-run                                                     | **Unverifiable** (this review). Parent-recorded.                                      |
| Final acceptance: one retained identity with verified custom MAIL FROM; no `test-a` stage                                                                                                                                                                      | SES + empty Emailer state                                                                                | Source still one retained identity. Live SES/`test-a` inventory not re-read. Evidence `:130, :190` claims destroy complete.                                                                                                                                                                                                                                                                                                                                                                                      | Not re-probed                                                             | **Unverifiable**                                                                      |
| ADR-0010 / plan end-state retained-record count "six" vs enumeration of seven                                                                                                                                                                                  | Either a count of six with six listed, or a documented correction                                        | Baseline ADR-0010 `:36` said "three to six" while listing three DKIM + MX + SPF + DMARC + Cloudflare auth = seven. Working tree `:36` says seven. README lists all seven and never says six. Plan T4 Change `:150` and Final acceptance `:177` still say six; Handoff Deviations `:189` records the off-by-one and the README rule.                                                                                                                                                                              | Count of listed records                                                   | **Approved deviation**                                                                |

### Approvals and conflicts

- **Approved deviation:** Six vs seven retained records. Source: work-document Handoff Deviations (`.adr/work/deliverability.md:189`), correcting accepted ADR-0010 Consequences which already enumerated seven records while counting six. Scope: the count word in ADR-0010 and the T4/Final-acceptance "six" wording. Rationale: clerical off-by-one (MX and SPF were collapsed, or the Cloudflare TXT was dropped from the count). Consequence: ADR-0010 now says seven; README lists the seven records without claiming six; plan end-state text still says six and is acknowledged there. ADR conventions allow clerical corrections in place. Not a convenience descope: listing six would have been the false inventory.
- **Authority conflict:** none remaining. T4 Change "list all six records" is the same off-by-one the deviation covers. ADR-0002 Authority still says "the rest are not discharged yet"; that pre-existing header line was out of T4's edit list and sits beside the new "Obligations discharged in part" line. Not treated as a conflict: body preserved by instruction.

## Follow-up closure

- **Round and material delta:** First implementation review (not a follow-up). Compared working tree to `cb73c3a` / `main` and to the accepted plan/ADR-0010.
- **Closure state:** Clear
- **Resolved or withdrawn:** n/a (no prior implementation-review findings)
- **Still material:** none
- **New fix-caused or fix-exposed findings:** none

## Findings

No material findings.

## Context-dependent concerns

- **Concern:** README Sending identity still says deploy `EmailerSending` then publish the records (`README.md:178`). DKIM CNAMEs must follow identity creation (tokens come from `GetEmailIdentity`). MAIL FROM / SPF / DMARC / `_report._dmarc` do not, and the plan reordered T1 before T2 so SES's first MX probe would not see NXDOMAIN under the zone's 86400 s negative TTL. A greenfield operator following README on a new account would set `mailFromDomain` before those records exist.
- **Disposition:** Not admitted. T4's Change text did not require reordering README; readiness still requires `MailFromDomainStatus=SUCCESS` before sending-ready; `USE_DEFAULT_VALUE` keeps mail flowing (unaligned SPF) until SUCCESS; wiki `:18` records the negative-cache rule. This account already published T1 first.

- **Concern:** T3 Evidence paraphrases `Authentication-Results` with ellipses and does not paste the raw header block or name `ARC-Authentication-Results`.
- **Disposition:** Not admitted. The Tests name `Authentication-Results: mx.cloudflare.net;` and five assertions; every assertion is present in the paraphrase; the ARC twin is parenthetical context, not a sixth required quote. Same evidence shape as the sending-identity live gate. Confidence is limited because this review was forbidden to re-read the operator mailbox.

## Confirmed-good areas

- MAIL FROM props are exactly `` mailFromDomain: `bounce.${emailIdentity}` `` and `mailFromBehaviorOnMxFailure: "USE_DEFAULT_VALUE"` (`stacks/sending-identity.ts:27-28`). No third output, no new env key.
- Alchemy `EmailIdentity.diff` still replaces only on identity-name change; MAIL FROM is reconcile step 3e; DKIM 3b/3c are skipped when those props are absent. An undefined provider diff falls back to `havePropsChanged` → `update`.
- Current public DNS matches T1 Verify at both the authoritative nameserver and `8.8.8.8`, including a single MX and a DMARC record without `aspf=s`.
- ADR-0009 `:29` still holds the accepted "three DKIM `CNAME`" rationale; `:47`/`:49` match T4's specified rewordings; T4 greps are empty.
- ADR-0002 body is byte-for-byte unchanged aside from the one required header line.
- Diff contains no application, Emailer-stack, or mass-sending-lane code.

## Limitations and caveats

- Live SES MAIL FROM status, DKIM timestamp, Alchemy plan/deploy/state, Route 53 change-batch type, Cloudflare API record metadata, T3 raw headers, and post-destroy inventory were not re-observed. Rows above mark that limit as **Unverifiable** rather than converting it into a finding.
- T3 token match is against the work document's paraphrases, not a header dump in the repo.
- Full `pnpm check` (claimed 470 unit tests) was not re-run; format and stack-file type-aware lint were.
- Work-document task Status labels (T1–T3 Verified, T4 In progress) were ignored as proof.

## Next steps

1. Owner may close this review as Clear and proceed to PR. No implementation changes are required by this review.
2. Optional, not required: paste the T3 raw `Authentication-Results` block next to the paraphrase if a later reader should not have to trust ellipses; consider telling a greenfield README reader to publish MAIL FROM/SPF/DMARC/`_report._dmarc` before the first shared deploy that sets `mailFromDomain`.
